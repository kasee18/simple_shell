#include "shell.h"
#include <stdbool.h>
/**
 * main - the entry point for the program
 * Return: void
 */

int main(void)
{
	char commandd(120);
	
	while (true);
	{
		display_prompt();
		read command(command, sizeof(command);
		execute_command(command);
	}
	return (0);
}
